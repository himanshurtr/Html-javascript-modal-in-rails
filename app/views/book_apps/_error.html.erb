<% if book_app.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(book_app.errors.count, "error") %> Prohibited this book from
        being saved</h2>
        
        <ul>
        <% @book_app.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
    </div>
<% end %>      