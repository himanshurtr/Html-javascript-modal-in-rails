<p id="notice"><%= notice %></p>

<b><h1 class='text-center'>Books</h1></b><br>
<center>
  <table class="table table-striped table-hover" style="width:1000px">
    <thead>
      <tr style="border:1px solid black; background-color:skyblue">
        <th class='kuch-bhi' scope="col">Title</th>
        <th class='kuch-bhi' scope="col">Price</th>
        <th colspan="3" style="text-align:center">Actions</th>
      </tr>
    </thead>

    <tbody class="bookTab">
      <% @book_apps.each do |book_app| %>
        <%= render 'book', book_app: book_app %> 
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Book App', new_book_app_path, remote: true %> 
</center>
